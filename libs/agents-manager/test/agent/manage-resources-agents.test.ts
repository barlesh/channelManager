import "jest";
import { Agent, AgentsManager } from "../../src/agents";
import * as uid from "uuid";
import { mockResourceManager } from "../mock/resource-manager.mock";

describe("Demo", () => {
  let agent;


  beforeEach(() => {
    // const agentObj = {
    //   name: "aname"
    // };
    // const resource = {
    //   name: "rname"
    // };
    // const uidS: string = uid();
    // const myResourceMnager = mockResourceMnager;
    // agent = new Agent(agentObj, );
    // agent.addResource(resource, uidS);
  });

  test("attach agent to resource > test 1", () => {
    
  });

  
});
